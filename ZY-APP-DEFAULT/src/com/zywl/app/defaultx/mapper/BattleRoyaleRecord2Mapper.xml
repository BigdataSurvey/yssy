<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zywl.app.defaultx.mapper.BattleRoyaleRecord2Mapper">

	<insert id="insert" parameterType="BattleRoyale2Record" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
		insert into r_battle_royale2_record
		(
			`user_id`,
			`order_no`,
			`periods_num`,
			`bet_info`,
			`bet_amount`,
			`profit`,
			`lottery_result`,
			`win_or_lose`,
			`status`,
			`create_time`,
			`update_time`


		)
		values
			(
				#{userId},
				#{orderNo},
				#{periodsNum},
				#{betInfo},
				#{betAmount},
				#{profit},
				#{lotteryResult},
				#{winOrLose},
				#{status},
				#{createTime},
				#{updateTime}




			)
	</insert>



	<select id="findOne" resultType="BattleRoyale2Record">
		select * from r_battle_royale2_record where id = #{id}
	</select>

	<select id="findByUserId" resultType="BattleRoyale2Record">
		select * from r_battle_royale2_record where user_id = #{userId} and status=1 order by id desc
		<if test="start != null and limit != null">
			limit #{start}, #{limit}
		</if>
	</select>

	<select id="findByOrderNo" resultType="BattleRoyale2Record">
		select * from r_battle_royale2_record where order_no = #{orderNo} limit 1
	</select>

	<select id="sumTotalsByUserId" resultType="java.util.Map">
		select
			ifnull(sum(bet_amount), 0) as totalInvest,
			ifnull(sum(profit), 0) as totalGain
		from r_battle_royale2_record
		where user_id = #{userId}
	</select>

	<select id="findAll" resultType="BattleRoyale2Record">
		select * from r_battle_royale2_record
	</select>

	<sql id="conditions">
		<if test="date != null">
			and date=#{date}
		</if>


		<if test="userId != null">
			and user_id = #{userId}
		</if>

	</sql>

	<select id="countByConditions" resultType="Long">
		select count(1) from r_battle_royale2_record
		<where>
			<include refid="conditions"></include>
		</where>
	</select>


	<select id="findNoPrize" resultType="BattleRoyale2Record">
		select *  from r_battle_royale2_record where status=1
	</select>

	<select id="findPeriodsNum" resultType="BattleRoyale2Record">
		select *  from r_battle_royale2_record where status=1 order by id desc limit 1
	</select>


	<select id="findByConditions" resultType="BattleRoyale2Record">
		select * from r_battle_royale2_record
		<where>
			<include refid="conditions"></include>
		</where>

		ORDER BY create_time desc
		<if test="start != null and limit != null">
			limit #{start}, #{limit}
		</if>
	</select>

	<update id="batchUpdateRecord"  parameterType="java.util.List">
		<foreach collection="list" item="item" index="index" open="" close="" separator=";">
			update r_battle_royale2_record
			<set>
				profit=#{item.winAmount},lottery_result=#{item.lotteryResult},win_or_lose=#{item.isWin},status=1,bet_info=#{item.betInfo},bet_amount=#{item.betAmount}
			</set>
			where order_no = #{item.orderNo}
		</foreach>
	</update>

	<update id="addBetAmount"  parameterType="BattleRoyale2Record">
		update r_battle_royale2_record set bet_amount = bet_amount+#{betAmount} where order_no = #{orderNo}

	</update>

	<delete id="deletedThreeDayRecord">
		delete from r_battle_royale2_record where create_time &lt;#{time}
	</delete>

	<update id="addBetAmountAndInfo"  parameterType="java.util.Map">
		update r_battle_royale2_record
		set bet_amount = bet_amount+#{betAmount}, bet_info=#{betInfo}
		where order_no = #{orderNo}
	</update>

	<select id="findRecentPeriodsDistinct" resultType="java.util.Map">
		select
			periods_num as periodsNum,
			max(lottery_result) as lotteryResult,
			max(id) as maxId
		from r_battle_royale2_record
		where status = 1
		  and lottery_result is not null
		  and lottery_result != ''
		group by periods_num
		order by maxId desc
			limit #{limit}
	</select>

	<!-- ✅按【期号 + 用户】批量结算该期所有未结算记录（下注阶段 orderNo 每次唯一，结算不能再按固定 orderNo 更新） -->
	<update id="batchUpdateRecordByPeriodUser"  parameterType="java.util.List">
		<foreach collection="list" item="item" index="index" open="" close="" separator=";">
			update r_battle_royale2_record
			<set>
				profit=#{item.winAmount},lottery_result=#{item.lotteryResult},win_or_lose=#{item.isWin},status=1
			</set>
			where periods_num = #{item.periodNo}
			and user_id = #{item.userId}
			and status = 0
		</foreach>
	</update>


	<!-- ✅按【期号 + 用户】聚合下注：存在则累加 bet_amount，并合并 bet_info（简单合并：直接覆盖同 elementId 的金额字符串） -->
	<update id="addBetAmountAndMergeInfoByPeriodUser" parameterType="java.util.Map">
		update r_battle_royale2_record
		set
			bet_amount = bet_amount + #{chip},
			bet_info = #{betInfo},
			update_time = #{now}
		where periods_num = #{periodNo}
		  and user_id = #{userId}
		  and status = 0
			limit 1
	</update>






	<!-- 查该用户该期未结算聚合记录（只取 id + bet_info） -->
	<select id="findUnsettledIdAndInfoByPeriodUser" resultType="java.util.Map">
		select id as id, bet_info as betInfo
		from r_battle_royale2_record
		where periods_num = #{periodNo}
		  and user_id = #{userId}
		  and status = 0
		order by id desc
			limit 1
	</select>

	<!-- 按主键更新：bet_amount += chip，bet_info 合并后的 JSON 直接写入 -->
	<update id="addBetAmountAndMergeInfoById" parameterType="java.util.Map">
		update r_battle_royale2_record
		set
			bet_amount = bet_amount + #{chip},
			bet_info = #{betInfo},
			update_time = #{now}
		where id = #{id}
			limit 1
	</update>
</mapper>
